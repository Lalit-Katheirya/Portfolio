services:
  - type: web
    name: portfolio
    env: node
    plan: free
    buildCommand: npm install --include=dev && npm run build && mkdir -p server/public/dist && cp -r dist/* server/public/dist/
    startCommand: npm run dev
    envVars:
      - key: NODE_ENV
        value: production